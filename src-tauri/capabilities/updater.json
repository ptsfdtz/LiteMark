{
  "$schema": "https://schema.tauri.app/config/2.0/capabilities.json",
  "identifier": "updater",
  "description": "Allow signed application updates and restart after installation",
  "windows": ["main"],
  "permissions": ["updater:default", "process:allow-restart"]
}
