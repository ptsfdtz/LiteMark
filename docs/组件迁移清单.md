# 组件迁移清单（旧组件 -> shadcn/ui）

> 当前仓库属于全新初始化，历史业务组件已移除。本清单按“目标功能组件”制定。

## 迁移映射表

| 目标功能组件 | 目标路径（建议） | shadcn/ui 组合 | 状态 | 备注 |
| --- | --- | --- | --- | --- |
| 顶部工具栏 | `src/features/editor/Toolbar.tsx` | `Button` + `Separator` + `DropdownMenu` | 待实现 | Markdown 工具按钮集合 |
| 编辑器容器 | `src/features/editor/EditorPane.tsx` | `Card` + `Textarea`（或 Monaco 容器） | 待实现 | 后续可切 Monaco |
| 预览容器 | `src/features/preview/PreviewPane.tsx` | `Card` + `Separator` | 待实现 | 负责 Markdown 输出 |
| 最近文件侧栏 | `src/features/file-manager/RecentFilesPanel.tsx` | `Sheet` + `ScrollArea` + `AlertDialog` | 待实现 | 包含删除确认 |
| 设置面板 | `src/features/settings/SettingsSheet.tsx` | `Sheet` + `Switch` + `Select` + `Input` | 待实现 | 主题/语言/路径设置 |
| 文件重命名 | `src/features/file-manager/RenameDialog.tsx` | `Dialog` + `Input` + `Button` | 待实现 | 对齐验证规则 |
| 保存提示 | `src/shared/ui/SaveToast.tsx` | `Toast` | 待实现 | 替代临时提示 |
| 窗口控制 | `src/features/window/WindowControls.tsx` | `Button` + `Tooltip` | 待实现 | Tauri 桌面专用 |
| 快捷键帮助 | `src/features/help/ShortcutsDialog.tsx` | `Dialog` + `Table` | 待实现 | 统一快捷键文案 |
| 状态栏 | `src/features/status/StatusBar.tsx` | `Badge` + `Separator` | 待实现 | 展示字数/行数 |

## 当前可复用基础组件（已存在）

1. `src/components/ui/button.tsx`
2. `src/components/ui/card.tsx`
3. `src/components/ui/select.tsx`
4. `src/components/ui/dropdown-menu.tsx`
5. `src/components/ui/alert-dialog.tsx`
6. `src/components/ui/input.tsx`
7. `src/components/ui/textarea.tsx`
8. `src/components/ui/separator.tsx`
9. `src/components/ui/badge.tsx`

## 完成度统计

当前完成度（2026-03-05）：
1. 业务组件迁移完成度：0/10（0%）
2. 基础 UI 原子组件可用度：9/10（90%）

## 验收口径

1. 业务页面不得直接使用原生 `button/input/select`，应使用 `src/components/ui/*`。
2. 每个业务组件迁移后必须补充最小测试与交互手测记录。
