# 组件迁移清单（旧组件 -> shadcn/ui）

> 当前仓库属于全新初始化，历史业务组件已移除。本清单按“目标功能组件”制定。

## 当前阶段策略（先不拆业务组件）

1. 业务先集中在 `App.tsx`，不强行拆成多个业务组件文件。
2. 仅基础能力使用 `components/ui/*`。
3. 业务组件拆分将在功能稳定后进行。

## 迁移映射表（后续阶段）

| 目标功能组件 | 目标路径（建议） | shadcn/ui 组合 | 状态 | 备注 |
| --- | --- | --- | --- | --- |
| 顶部工具栏 | `src/App.tsx`（后续可拆） | `Button` + `Separator` + `DropdownMenu` | 待实现 | 先在 App 内实现 |
| 编辑区（实时渲染） | `src/App.tsx`（后续可拆） | `Card` + 编辑器内核 | 进行中 | 编辑器即预览 |
| 最近文件侧栏 | `src/App.tsx`（后续可拆） | `Sheet` + `ScrollArea` + `AlertDialog` | 待实现 | 包含删除确认 |
| 设置面板 | `src/App.tsx`（后续可拆） | `Sheet` + `Switch` + `Select` + `Input` | 待实现 | 主题/语言/路径设置 |
| 保存提示 | `src/App.tsx`（后续可拆） | `Toast` | 待实现 | 替代临时提示 |
| 窗口控制 | `src/App.tsx`（后续可拆） | `Button` + `Tooltip` | 待实现 | Tauri 桌面专用 |
| 快捷键帮助 | `src/App.tsx`（后续可拆） | `Dialog` + `Table` | 待实现 | 统一快捷键文案 |
| 状态栏 | `src/App.tsx`（后续可拆） | `Badge` + `Separator` | 待实现 | 展示字数/行数 |

## 当前可复用基础组件（已存在）

1. `src/components/ui/button.tsx`
2. `src/components/ui/card.tsx`
3. `src/components/ui/select.tsx`
4. `src/components/ui/dropdown-menu.tsx`
5. `src/components/ui/alert-dialog.tsx`
6. `src/components/ui/input.tsx`
7. `src/components/ui/textarea.tsx`
8. `src/components/ui/separator.tsx`
9. `src/components/ui/badge.tsx`

## 完成度统计

当前完成度（2026-03-05）：
1. 业务组件迁移完成度：按“App 内实现”推进，暂不按文件拆分统计
2. 基础 UI 原子组件可用度：9/10（90%）

## 验收口径

1. 业务页面不得直接使用原生 `button/input/select`，应使用 `src/components/ui/*`。
2. 每个业务组件迁移后必须补充最小测试与交互手测记录。
